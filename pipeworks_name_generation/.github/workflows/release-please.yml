# Release Please Workflow for pipeworks_name_generation
# Uses centralized reusable workflow from pipe-works/.github

name: Release Please

on:
  push:
    branches: [main]

jobs:
  release:
    uses: pipe-works/.github/.github/workflows/reusable-release-please.yml@main
    with:
      release-type: 'python'
      package-name: 'pipeworks-name-generation'
      bump-minor-pre-major: true
      bump-patch-for-minor-pre-major: true
    secrets: inherit
